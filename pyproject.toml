[project]
name = "ai-test-bench-python"
version = "0.1.0"
description = "Performance benchmarking for AI analyzer"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest-codspeed>=4.3.0",
]
